export declare class Matcher {
    private readonly sorted;
    constructor(networks?: string[]);
    has(network: string): boolean;
    get(network: string): string | null;
    add(network: string): this;
}
